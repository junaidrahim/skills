# Data Ingestion

Put versioned ingestion scripts here. Each script must load data into `research.duckdb` and register the dataset ID, version, source, content hash, row count, unit of analysis, parser version, and distribution notes.

Do not create another database file. Keep a raw source file only when acquisition, size, or license rules require it.
